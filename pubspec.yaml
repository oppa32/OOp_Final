name: navalbiz_tracker
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  dartz: ^0.10.1
  google_fonts: ^6.3.2
  flutter_form_builder: ^10.1.0
  form_builder_validators: ^11.2.0
  
  # CHANGED: Downgraded to stable 2.x to support StateNotifierProvider
  flutter_riverpod: ^2.6.1
  
  intl: ^0.20.2
  uuid: ^4.5.2
  
  # REMOVED: state_notifier (Riverpod 2.6.1 handles this internally)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.5.4

flutter:
  uses-material-design: true